<feedback-sessions-title></feedback-sessions-title>

<feedback-sessions
  initial-type="provider"
  :initial-sessions="<%= serialize_resource(@provider_sessions) %>"
  :tags="<%= serialize_resource(@tags) %>"
></feedback-sessions>
